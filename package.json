{
  "name": "root",
  "private": true,
  "scripts": {
    "outdated": "(npm outdated || true) && lerna exec --stream --no-bail -- npm outdated",
    "remove": "lerna exec --stream --no-bail -- rm -rf node_modules package-lock.json *.tgz && rm -rf node_modules package-lock.json  *.tgz",
    "reinstall": "npm run remove && npm run update",
    "update": "npm install --legacy-peer-deps && lerna exec --ignore=@template/* --stream -- npm install --legacy-peer-deps && npm update --legacy-peer-deps && lerna exec --ignore=@template/* --stream -- npm update --legacy-peer-deps"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "devDependencies": {
    "@leanup/cli": "^1.1.0-rc.170",
    "@leanup/git-hooks": "1.1.0-rc.155",
    "lerna": "^3.22.1",
    "typescript": "^4.1.2"
  }
}
