IVersion:
  type: object
  properties:
    major:
      type: integer
      format: int64
      example: 1
    minor:
      type: integer
      format: int64
      example: 0
    patch:
      type: integer
      format: int64
      example: 0
    text:
      type: string
      example: 1.0.0
    label:
      type: string
      enum:
        - latest
        - next
        - rc
        - beta
        - alpha
