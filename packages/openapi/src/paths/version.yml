get:
  tags:
    - Version
  summery: Version
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: '../models/version.yml#/IVersion'
